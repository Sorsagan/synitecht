const { PermissionFlagsBits } = require("discord.js");

module.exports = {
  customId: "voiceChannelRemoveUserBtn",
  userPermissions: [],
  botPermissions: [],

  run: async (client, interaction) => {},
};
